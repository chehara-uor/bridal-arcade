/* eslint-disable @typescript-eslint/no-explicit-any */
import { normalizeSriLankanMobile, verifyOtpChallenge } from "./_otp.js";
import { createSession, readJson, sendJson, sessionCookie, wordpressJson, type PortalUser } from "./_session.js";
export default async function handler(request:any,response:any){if(request.method!=="POST")return sendJson(response,405,{message:"Method not allowed."});try{const body=await readJson(request);const email=typeof body.email==="string"?body.email.trim().toLowerCase():"";const phone=normalizeSriLankanMobile(typeof body.phone==="string"?body.phone:"");const name=typeof body.name==="string"?body.name.trim():"";const password=typeof body.password==="string"?body.password:"";const otp=typeof body.otp==="string"?body.otp:"";const challenge=typeof body.challenge==="string"?body.challenge:"";if(!phone||!verifyOtpChallenge(challenge,otp,email,phone))return sendJson(response,400,{message:"The verification code is invalid or expired."});if(name.length<2||password.length<8)return sendJson(response,400,{message:"Valid account details are required."});const registered=await wordpressJson("/wp-json/bridal/v2/self-register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password,first_name:name,phone,account_type:"individual"})});if(!registered.response.ok)return sendJson(response,registered.response.status,{message:registered.data?.message||"Unable to create your account."});const wpUser=registered.data?.user;if(!wpUser?.id||!wpUser?.email)return sendJson(response,502,{message:"WordPress returned an incomplete registration response."});const user:PortalUser={id:String(wpUser.id),email:String(wpUser.email),name:String(wpUser.first_name||name),roles:Array.isArray(wpUser.roles)?wpUser.roles.map(String):[],account_type:"individual"};response.setHeader("Set-Cookie",sessionCookie(createSession(user)));return sendJson(response,201,{user});}catch{return sendJson(response,502,{message:"Unable to complete your verified registration."});}}
